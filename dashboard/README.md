# Dashboard Module

The production dashboard lives in `frontend/src/pages/DashboardPage.tsx` and related components.

This folder is reserved for future dashboard-specific assets, analytics widgets, saved reports, and admin views as LeadHive grows into a larger SaaS.

